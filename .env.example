# ReMap Development Environment Configuration
# Copy this file to .env and customize as needed for your local setup
# Most team members won't need to modify anything here!

# Database Configuration (automatically configured by docker-compose)
DB_NAME=remap_dev
DB_USER=remap_user
DB_PASSWORD=dev_password

# Backend API Configuration
JWT_SECRET=your_very_long_random_jwt_secret_here_at_least_64_characters
NODE_ENV=development

# Network Configuration (automatically detected by startup script)
# HOST_IP will be automatically set by the development startup script
# You can override this if automatic detection doesn't work for your network
# HOST_IP=192.168.1.100  # Uncomment and set manually if needed

# Development Mode Settings
# These control how the Expo development server behaves
EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
# REACT_NATIVE_PACKAGER_HOSTNAME is set automatically

# Optional: Override default behavior
# USE_TUNNEL=true  # Uncomment to always use tunnel mode (slower but more reliable)
# USE_LAN=true     # Uncomment to force LAN mode (faster but requires same network)

# Future API integrations (add as needed)
# GOOGLE_MAPS_API_KEY=your_google_maps_key_here
# MAPBOX_ACCESS_TOKEN=your_mapbox_token_here